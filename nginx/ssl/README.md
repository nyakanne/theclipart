# HTTPS Certificates

Production Nginx expects these files in this directory:

- `cert.pem`
- `key.pem`

Use real certificates from your host, Cloudflare origin certs, or Let's Encrypt.
Do not commit private keys.
