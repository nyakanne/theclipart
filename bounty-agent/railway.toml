[build]
builder = "nixpacks"

[deploy]
startCommand = "pip install -r ui/backend/requirements.txt && python ui/backend/app.py"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[healthcheck]
path = "/health"
timeout = 30
